name: SwiftProof PR pilot
on:
  pull_request:
permissions:
  contents: read
concurrency:
  group: swiftproof-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  evidence:
    uses: ./.github/workflows/review.yml
    with:
      enforce: false
      reviewer: false
